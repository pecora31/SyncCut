Select a separately prepared runtime folder in SyncCut > Runtime.

Run the bundled engine/setup-runtime.ps1 on the customer computer; see docs/customer-validation.md.
Models and Python packages are not included in the editor installer and never downloaded by processing jobs.
The setup creates a Windows venv tied to the customer's installed CPython. It is not a portable Python distribution.
